ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
export PATH="/usr/local/bin:$PATH"
brew install node
brew install git
npm install cordova
\curl -sSL https://get.rvm.io | bash
source /Users/jeezs/.rvm/scripts/rvm
rvm reload
rvm install 2.7
rvm use 2.7 --default
gem install bundler roda sqlite3 sequel rack-unreloader faye-websocket puma websocket-extensions rack --no-document
