#Github usage
# to allow large file storage install:
#install brew install git-lfs
#Select the file types you'd like Git LFS to manage (or directly edit your .gitattributes). You can configure additional file extensions at anytime.
git lfs track "*.zip"
git lfs track "*.app"

Make sure .gitattributes is tracked
git add .gitattributes




#Create a new repository on GitHub. To avoid errors, do not initialize the new repository with README, license, or gitignore files


#Open Terminal.
#Change the current working directory to your local project.
#Initialize the local directory as a Git repository.
$ git init

$ git add .
# Adds the files in the local repository and stages them for commit. To unstage a file, use 'git reset HEAD YOUR-FILE'.

$ git commit -m "First commit"
# Commits the tracked changes and prepares them to be pushed to a remote repository. To remove this commit and modify the file, use 'git reset --soft HEAD~1' and commit and add the file again.

#copy the remote repository URL.

#In Terminal, add the URL for the remote repository where your local repository will be pushed.
$ git remote add origin "remote_URL"
# if git send "fatal: remote origin already exists." the git remote rm origin solve the problem
# Sets the new remote
$ git remote -v
# Verifies the new remote URL

$ git push -u origin master
# Pushes the changes in your local repository up to the remote repository you specified as the origin

#to sum up:
#Create a new repository on GitHub. To avoid errors, do not initialize the new repository with README, license, or gitignore files
git init
git add .
git commit -m "First commit"
git remote add origin https://github.com/atome-fr/atome.git
git push -u origin master





#gitalite usage 

#create SSH key pair 
ssh-keygen -t rsa
#ajouter la clef
ssh-add -K ~/.ssh/mbAir_rsa
#connect to server
ssh git@5.135.157.198    
#serveur renvoi
 R W	gitolite-admin
 R W	testing
quoi faire après? : git clone https://5.135.157.198/testing ?
réponse : 

git clone git@5.135.157.198:testing 


git clone git@5.135.157.198:gitolite-admin


